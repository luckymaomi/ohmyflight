# [任务名] Plan

## 1. 需求文档

用户要解决什么实际问题：

谁会使用：

用户完成任务时应该看到什么体验：

当前范围包含：

当前范围不包含：

业务上怎样算完成：

## 2. 当前事实

已确认代码事实：

已确认测试事实：

已确认文档/spec 事实：

已确认配置或入口事实：

已确认命令输出：

当前已有能力：

当前缺口：

仍不明确：

## 3. 失败测试

自动测试：

类型检查：

构建命令：

静态检查：

真实用户路径演练：

失败判定：

## 4. 目标

用户路径完成到：

代码主链路接到：

状态和记录落到：

输出呈现到：

测试和文档同步到：

完成判定：

## 5. 不做范围

不做：

不保留：

不兼容：

不处理：

## 6. 设计

主链路：

输入 -> 判断 -> 状态 -> 执行 -> 输出 -> 记录

模块边界：

文件职责：

状态归属：

数据或事件流：

错误、恢复、中断、重试边界：

与现有架构的关系：

测试和文档影响：

## 7. 实施任务

- [ ] T001 动作：；文件/模块：；依赖：；验收：
- [ ] T002 动作：；文件/模块：；依赖：；验收：
- [ ] T003 动作：；文件/模块：；依赖：；验收：

## 8. 验证计划

相关局部测试：

完整验证命令：

构建或安装检查：

页面/工具入口检查：

恢复或中断演练：

文档同步检查：

未验证内容：

剩余风险：

## 9. 收口

目标是否完成：

失败测试是否变绿：

改了哪些文件：

跑了哪些验证：

哪些内容没有验证：

剩余风险：

是否 commit / push：
